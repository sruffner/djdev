# Changelog

## v0.3.0 (08/04/2022)

- Added method `metadata_table()` in `sglportalpi.PortalAccessor` to retrieve the contents of selected "metadata" tables in the
portal database. 
- Added method `neurons()` in `sglportalapi.PortalAccessor` to retrieve information on all recorded neurons in the 
portal database that satisfy selected filter criteria.
- Methods in `sglportalapi.PortalAccessor` that retrieve trial data can now specify what response data 
(behavioral, neuronal, digital event timestamps) to return. This can reduce retrieval times when, for 
example, you are only interested in neuronal spike trains.
- Incremented API_VERSION to 2. **If you have already installed the sglportalapi package, you will need to 
upgrade to this release.**
- Updated `PL2.py` to handle PL2 files generated by Plexon software v1.19 or later. These are technical 
changes critical to the preprocessing of session archives; no impact on users of the portal API itself.

## v0.2.0 (06/21/2022)

- Eliminated use of the pickle module in serializing and deserializing API responses.
- A number of significant changes were made to the data containers exposed in the sglportalapi package, primarily to 
implement serialization of the containers without depending on the pickle module.

## v0.1.0 (05/11/2022)

- First release of `sglportalapi`